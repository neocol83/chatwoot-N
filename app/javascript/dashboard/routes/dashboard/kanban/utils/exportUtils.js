export const exportToCSV = (data, filename) => {
  // Implementação da exportação para CSV
  console.log('Exportando para CSV:', data, filename);
};

export const exportToPDF = (data, filename) => {
  // Implementação da exportação para PDF
  console.log('Exportando para PDF:', data, filename);
};

export const exportToExcel = (data, filename) => {
  // Implementação da exportação para Excel
  console.log('Exportando para Excel:', data, filename);
};
